namespace Build_IT_Infrastructure.Constants
{
    public static class Regions
    {
        #region Properties

        public const string CONTENT_REGION = nameof(CONTENT_REGION);
        public const string HEADER_REGION = nameof(HEADER_REGION);
        public const string SCRIPT_REGION = nameof(SCRIPT_REGION);
        public const string DETAILS_REGION = nameof(DETAILS_REGION);
        public const string NAVIGATION_TAB = nameof(NAVIGATION_TAB);

        #endregion // Properties
    }
}
